feat: add production features (retries, validation, datasource tool)

- Implemented get_datasources tool for datasource discovery and health monitoring
- Added RetryPolicy with exponential backoff for transient SQL failures
- Implemented RequestValidator to enforce safety limits on queries and procedures
- Integrated retries into QueryExecutor and ProcedureExecutor
- Integrated request validation into tool handlers
- Completed ITERATION 6 of the implementation plan (except for Microsoft.Extensions.Logging transition)
